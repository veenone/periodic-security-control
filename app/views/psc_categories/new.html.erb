<%= title l(:label_psc_category_new) %>

<%= form_tag(project_psc_categories_path(@project)) do %>
  <div class="box tabular">
    <%= render partial: 'form' %>
  </div>

  <p>
    <%= submit_tag l(:button_create) %>
    <%= link_to l(:button_cancel), project_psc_categories_path(@project) %>
  </p>
<% end %>
