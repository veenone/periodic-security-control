<%= title l(:label_psc_category_new) %>

<h2><%= l(:label_psc_category_new) %></h2>

<%= form_tag(psc_categories_path) do %>
  <div class="box tabular">
    <%= render partial: 'form' %>
  </div>

  <p>
    <%= submit_tag l(:button_create) %>
    <%= link_to l(:button_cancel), psc_categories_path %>
  </p>
<% end %>
